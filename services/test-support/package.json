{
  "name": "@artifactbin/test-support",
  "version": "0.1.0",
  "private": true,
  "description": "The fixtures every workspace's tests share: temp home/workspace roots, the real-socket HTTP server, one PGLite with schema reset and a minted token row, a mail sink, and the recorded browser headers. Runner-agnostic on purpose — the CLI suite runs under node:test, everything else under Vitest.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./net": "./src/net.ts",
    "./temp": "./src/temp.ts",
    "./db": "./src/db.ts",
    "./mail": "./src/mail.ts",
    "./browser": "./src/browser.ts"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "@electric-sql/pglite": "0.4.6"
  }
}
