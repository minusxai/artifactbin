/**
 * Where this software lives. The plugin has its own repo (lib/plugin-id) —
 * this is the SERVER's, the one a "source" or "self-host" link means.
 */
export const REPO_URL = 'https://github.com/minusxai/artifactbin';
