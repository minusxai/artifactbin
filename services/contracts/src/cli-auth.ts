/** Authentication vocabulary shared by CLI, proxy consent and app token minting. */
export const ARTIFACT_SCOPE = 'artifacts';
export const API_RESOURCE_PATH = '/api';
export const CLI_PROTOCOL_VERSION = 1;
