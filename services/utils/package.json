{
  "name": "@artifactbin/utils",
  "version": "0.1.0",
  "description": "The shared runtime the services are built with: assemble parts into a Hono app, serve it, the upstream seam (in-process or over HTTP), actor signing, env reads, JSON transports, logging, and the test doubles for every contract.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./shape": "./src/shape.ts"
  },
  "files": ["src"],
  "dependencies": {
    "@artifactbin/contracts": "*",
    "hono": "4.13.5",
    "@hono/node-server": "2.1.1"
  },
  "devDependencies": {
    "vitest": "4.1.11"
  }
}
