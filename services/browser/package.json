{
  "name": "@artifactbin/browser",
  "version": "0.1.0",
  "description": "The browser service: a Chromium that renders a URL to an image, in this process or in one of its own. `.` is the contract, the HTTP client and the server shell; `./local` is Playwright and the only entry that loads it.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./local": "./src/local.ts"
  },
  "files": ["src", "CONTRACT.md"],
  "dependencies": {
    "@artifactbin/contracts": "*",
    "@artifactbin/utils": "*",
    "playwright": "1.62.1",
    "sharp": "0.35.4"
  },
  "devDependencies": {
    "vitest": "4.1.11"
  }
}
