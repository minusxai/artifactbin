# Managed Iframe implementation

Approved direction: top-level artifact, trusted `i.` controls, opaque `srcdoc` author region inside a bootstrap-only protective wrapper. No per-artifact hostname, no sandbox relaxation, no Three.js exception.

## Ordered merge gates

1. Resolve startup observability/reliability with default browser security. MEASURED: missing child observation was false red; ocean parent telemetry passed cold/reloads, including independent30/30 root replay. Integrated wrapper navigation/readiness gates pass.
2. State delivery: initial snapshot, changed-field deltas, named listener subscriptions, bounded coalescing of state only, disposal. IMPLEMENTED/REVIEWED; red replay5fail, focused34pass, full6209app+1skip/7CLI, validate pass. Mutations remain ordered requests and are never coalesced.
3. Managed markup: static `<Iframe>` children compiled into isolated DOM and ordered scripts; parent never renders these children. IMPLEMENTED/REVIEWED; compiler removed17/17red, restored25pass. Server validation and read-time interpreter enforce the boundary; parent discovery and URL rewriting stop at Iframe. Existing documentation caps preserved.
4. Cached assets: IMPLEMENTED/REVIEWED. Reuse guarded importer/cache for bundled JS and supported assets; public read-only asset origin, no viewer authority, redirects or proxy endpoints. Review caught/fixed normalized IPv4-mapped IPv6 bypass, missing version keys, account quota and dev host routing. Public refs recheck anonymous ACL; private refs intentionally refused. Independent dev mint/create/resolve/GET/HEAD/host-denial flow passed.
5. Runtime integration: IMPLEMENTED/REVIEWED. Private port through wrapper, named data operations, compiled content loading, disposal and visible failures. Wrapper removal independently gives3failed/1passed; restored12focusedpassed. Actual publish/render gate passed Chromium/Firefox/WebKit, including raw mode, dynamic images/scripts, classic/module ordering, public refs, stable SSR height, navigation denial with positive control. Existing Helmet/Sandbox stay isolated.
6. Independent review: RED fault replays, validate, full suite/build and all53browser gates pass (150seconds across6servers, no retries). Final authenticated/private-preview/legacy-library controls gates independently pass Firefox and WebKit as well as Chromium. PR46 contains the implementation; its live CI/CodeQL checks remain merge gates, not production-rollout evidence.

No phase completion implies production rollout verification. HTTPS/DNS asset-host provisioning and staging cookies/device checks require deployment environment; report them separately from implemented/CI-tested code.

## Public surface

`<Iframe title="Scene" height={400}>` contains static HTML/CSS and scripts. External scripts are self-contained browser bundles, declared with their original HTTP(S) CDN URL; import/cache resolution happens before execution. No recursive module graph bundler. Script blocks use static JSX string children. Internal scripts use `mx.params.get/set`, `mx.params.subscribe(names, callback)`, `mx.data.get/subscribe`, and declared `mx.mutate`; these do not grant account, arbitrary authenticated HTTP, or parent DOM access.

All getters remain synchronous from an initial snapshot plus deltas; named subscriptions filter callbacks, not getter freshness. Existing callback-only subscriptions remain compatible. The browser composites each child canvas directly; pixels do not cross the bridge.

## Review measurements and corrections

- Actual product state probe: `node scripts/planning/managed-product-state.mjs <engine>`. All three engines pass1/4/8regions with0/10,000rows,100same-turn changes collapsed to1final delivery per region, plus30mount/dispose cycles with zero retained product subscriptions/frames. Serial medians Chromium15.6–19ms, Firefox16–18ms, WebKit16ms; the intentional16ms coalescing interval dominates. Synthetic producer, not database/network latency or screen paint.
- Full merged product checkpoint: validate and build pass; API1,326 + node3,830 + UI1,153 + CLI7 = 6,316 passed, one optional skip. Two additional diagnostic input-bound tests pass independently. Earlier seven regressions in docs contracts, gate manifest and generated classes were corrected before the successful rerun. The first full browser run passed50/53; stale bound-assets, full-kit and dataflow assertions were corrected while retaining exact asset identity and strict wrapper checks. Final run passes53/53 without retries.
- WebRTC: strict `connect-src 'none'` and even proposed `webrtc 'block'` still allow STUN UDP in tested Chromium/WebKit (4packets each, same as positive control). Immutable pre-author removal of standard/prefixed RTCPeerConnection constructors prevents tested constructor restoration/prototype/fresh-frame attempts. Core2tests observed RED before implementation; actual product bootstrap independently yields0nested packets versus4positive-control packets in Chromium/WebKit. Firefox's UDP positive control sends0, so its packet result is inconclusive; constructor refusal is still asserted. CSP alone is NOT the full network boundary.
- BrowserOS real-CDN canary: a Three.js0.160.1 bundle declared from jsDelivr loads through the generic asset pipeline, renders verified WebGL cube pixels, and responds to a real pointer click by running a local SQL mutation. Parent contains no canvas; reload succeeds. This is a manual compatibility canary, not a third-party-dependent merge gate.
- Firefox compatibility correction: inherited parent `script-src 'self'` refused legacy pinned bundles in nested opaque frames. The parent now explicitly permits only its existing `/libraries/` directory; inner policy still lists exact pinned URLs. Independent policy rollback gives1failed/10passed. No connect-src, navigation or account authority was added. WebKit keyboard fixture explicitly focuses its own button; actual Enter remains tested.
- CodeQL found an unbounded HTTP size parameter in the startup diagnostic, not the shipped runtime. Rejecting non-integer/out-of-range size and depth before allocation gives RED1failed/1passed → GREEN2passed. CodeQL reports no new alerts at68888af. No alert suppression was used.
